@model IEnumerable<Biblioteka.Egzemplarz>

@{
    ViewBag.Title = "Egzemplarzs";
}

<h2>Egzemplarzs</h2>

<p>
    @Html.ActionLink("Create New", "Create", "Back to List")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ksiazka.Tytul)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ksiazka.Tytul)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IdEgzemplarz }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdEgzemplarz }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdEgzemplarz })
            </td>
        </tr>
    }

</table>

<div>
    @Html.ActionLink("Back to List", "Index")
</div>