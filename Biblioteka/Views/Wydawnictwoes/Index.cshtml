@model IEnumerable<Biblioteka.Wydawnictwo>

@{
    ViewBag.Title = "Index";
}

<h2>Wydawnictwa</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdWydawnictwo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdWydawnictwo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdWydawnictwo })
        </td>
    </tr>
}

</table>
